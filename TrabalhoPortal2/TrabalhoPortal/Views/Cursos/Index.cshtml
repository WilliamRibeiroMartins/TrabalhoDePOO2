@model IEnumerable<Portal.Models.Curso>

@{
    ViewBag.Title = "Cursos";
}

<h2>Cursos</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.codcurso }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.codcurso }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.codcurso }) |
						@Html.ActionLink("Associar Disciplina", "AssociarDisc", new { id = item.codcurso })
        </td>
    </tr>
}

</table>
