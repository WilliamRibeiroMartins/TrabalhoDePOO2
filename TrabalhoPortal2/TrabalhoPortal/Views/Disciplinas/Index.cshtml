@model IEnumerable<Portal.Models.Disciplina>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Nome
        </th>
        <th>
            Professor
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        @if (item.professor != null)
        {
        <td>
            @Html.DisplayFor(modelItem => item.professor.nome)
        </td>
        }
        else
        {
        <td>
            
        </td>
        }
        <td>
            @Html.ActionLink("Associar Professor", "AssociarProf", new { id = item.coddisciplina }) |
            @Html.ActionLink("Editar", "Edit", new { id=item.coddisciplina }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.coddisciplina }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.coddisciplina })
        </td>
    </tr>
}

</table>
